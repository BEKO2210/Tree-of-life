const DPR=Math.min(devicePixelRatio||1,2);const sky=document.getElementById('sky'),tree=document.getElementById('tree'),energy=document.getElementById('energy');const boost=document.getElementById('boost');let W=0,H=0,t=0,awake=0,pointer={x:.5,y:.5};const rnd=s=>{let x=Math.sin(s)*10000;return x-Math.floor(x)};function size(c){c.width=W*DPR;c.height=H*DPR;c.style.width=W+'px';c.style.height=H+'px';c.getContext('2d').setTransform(DPR,0,0,DPR,0,0)}function resize(){W=innerWidth;H=innerHeight;[sky,tree,energy].forEach(size);drawSky();drawTree()}addEventListener('resize',resize);addEventListener('pointermove',e=>{pointer.x=e.clientX/W;pointer.y=e.clientY/H},{passive:true});boost?.addEventListener('click',()=>awake=1);function branch(ctx,x,y,len,ang,w,depth,seed){if(depth<=0||w<.42)return;const bend=(rnd(seed)-.5)*.48;const x2=x+Math.cos(ang+bend)*len;const y2=y+Math.sin(ang+bend)*len;const g=ctx.createLinearGradient(x,y,x2,y2);g.addColorStop(0,`rgba(${90+depth*11},${50+depth*7},26,.98)`);g.addColorStop(.56,`rgba(${151+depth*6},${91+depth*5},39,.92)`);g.addColorStop(1,`rgba(245,190,95,.72)`);ctx.strokeStyle=g;ctx.lineWidth=w;ctx.lineCap='round';ctx.shadowColor='rgba(255,198,98,.14)';ctx.shadowBlur=w*1.9;ctx.beginPath();ctx.moveTo(x,y);const mx=(x+x2)/2+Math.cos(ang-Math.PI/2)*len*.14*Math.sin(seed);const my=(y+y2)/2+Math.sin(ang-Math.PI/2)*len*.14*Math.cos(seed);ctx.quadraticCurveTo(mx,my,x2,y2);ctx.stroke();if(depth<7){for(let i=0;i<2+(rnd(seed+2)>.55?1:0);i++){const leafAng=ang+(rnd(seed+i*3)-.5)*1.6;leaf(ctx,x2+Math.cos(leafAng)*rnd(seed+i)*8,y2+Math.sin(leafAng)*rnd(seed+i)*8,3+rnd(seed+i*7)*8,seed+i)}}const forks=depth>8?3:2+(rnd(seed+9)>.62?1:0);for(let i=0;i<forks;i++){const spread=(-.52+(i/(Math.max(1,forks-1)))*1.04)+(rnd(seed+i+12)-.5)*.34;branch(ctx,x2,y2,len*(.67+rnd(seed+i+30)*.16),ang+spread,w*(.62+rnd(seed+i+44)*.08),depth-1,seed*1.31+i*8.7)}}function leaf(ctx,x,y,r,s){const hue=rnd(s)>0.5?'125,255,191':'255,216,122';ctx.save();ctx.translate(x,y);ctx.rotate(rnd(s+1)*Math.PI);ctx.fillStyle=`rgba(${hue},${.25+rnd(s+2)*.45})`;ctx.shadowColor=`rgba(${hue},.5)`;ctx.shadowBlur=12;ctx.beginPath();ctx.ellipse(0,0,r*.42,r,0,0,Math.PI*2);ctx.fill();ctx.restore()}function drawTree(){const ctx=tree.getContext('2d');ctx.clearRect(0,0,W,H);ctx.save();ctx.translate(W/2,H*.9);const scale=Math.min(W/1200,H/820)*1.12;ctx.scale(scale,scale);const halo=ctx.createRadialGradient(0,-360,30,0,-360,560);halo.addColorStop(0,'rgba(255,216,122,.24)');halo.addColorStop(.38,'rgba(125,255,191,.13)');halo.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=halo;ctx.fillRect(-700,-940,1400,1200);for(let i=0;i<30;i++){const a=-Math.PI/2+(i-15)*.024;branch(ctx,0,0,220+rnd(i)*130,a,30+rnd(i+8)*12,11,i+4)}for(let i=0;i<70;i++){const a=Math.PI+(i/69)*Math.PI;const r=80+rnd(i+100)*520;branch(ctx,0,4,r*.44,a+(rnd(i)-.5)*.8,10+rnd(i+4)*8,6,i+200)}ctx.restore()}function drawSky(){const ctx=sky.getContext('2d');ctx.clearRect(0,0,W,H);const g=ctx.createLinearGradient(0,0,0,H);g.addColorStop(0,'#050713');g.addColorStop(.43,'#101a3a');g.addColorStop(1,'#02030a');ctx.fillStyle=g;ctx.fillRect(0,0,W,H);for(let i=0;i<520;i++){const x=rnd(i)*W,y=rnd(i+3)*H,r=.35+rnd(i+6)*1.5;ctx.fillStyle=`rgba(255,255,255,${.16+rnd(i+9)*.7})`;ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill()}for(let i=0;i<9;i++){const x=(.16+rnd(i)*.68)*W,y=(.12+rnd(i+1)*.55)*H,rr=90+rnd(i+2)*190;const neb=ctx.createRadialGradient(x,y,0,x,y,rr);neb.addColorStop(0,`rgba(${rnd(i)>0.5?'125,255,191':'188,167,255'},.16)`);neb.addColorStop(1,'rgba(0,0,0,0)');ctx.fillStyle=neb;ctx.fillRect(x-rr,y-rr,rr*2,rr*2)}}function vine(ctx,phase,rad,color){ctx.strokeStyle=color;ctx.lineWidth=1.2;ctx.beginPath();for(let i=0;i<220;i++){const p=i/219;const y=H*.93-p*H*.9;const twist=Math.sin(p*15+phase)*rad*(.2+p);const x=W/2+twist+Math.sin(p*34+phase*.7)*18;ctx[i?'lineTo':'moveTo'](x,y)}ctx.stroke()}function animate(){t+=.016;const ctx=energy.getContext('2d');ctx.clearRect(0,0,W,H);const parX=(pointer.x-.5)*18,parY=(pointer.y-.5)*10;tree.style.transform=`translate3d(${parX}px,${parY}px,0) scale(${1+awake*.018})`;sky.style.transform=`translate3d(${-parX*.32}px,${-parY*.22}px,0)`;const pulse=.45+.35*Math.sin(t*1.7)+awake*.35;ctx.globalCompositeOperation='screen';for(let i=0;i<5;i++)vine(ctx,t*1.4+i,70+i*24,`rgba(${i%2?'125,255,191':'255,216,122'},${.12+pulse*.18})`);for(let i=0;i<130;i++){const p=(rnd(i)*1.1+(t*(.018+rnd(i+1)*.035)))%1;const x=W/2+Math.sin(p*9+i)*W*(.08+p*.28);const y=H*.9-p*H*.82;const r=1+rnd(i+2)*3;ctx.fillStyle=`rgba(${rnd(i)>0.45?'255,216,122':'125,255,191'},${.18+pulse*.5})`;ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(animate)}resize();animate();
